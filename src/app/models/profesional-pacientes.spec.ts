import { ProfesionalPacientes } from './profesional-pacientes';

describe('ProfesionalPacientes', () => {
  it('should create an instance', () => {
    expect(new ProfesionalPacientes()).toBeTruthy();
  });
});
